package se.publicclass.example.command {	import org.puremvc.as3.patterns.command.MacroCommand;	/**	 * @author Jakob Stenqvist, jakob(at)publicclass.se	 */	public class StartupCommand extends MacroCommand {		override protected function initializeMacroCommand() : void {			addSubCommand( SetupModelCommand );			addSubCommand( SetupViewCommand );			 // We put this last because it'll call the initial route			addSubCommand( SetupRoutesCommand );		}	}}